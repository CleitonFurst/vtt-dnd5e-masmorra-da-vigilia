{
  "name": "vtt-dnd5e",
  "version": "0.1.0",
  "private": true,
  "description": "Virtual Tabletop para D&D 5e — monorepo (web + server + shared)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n SERVER,WEB -c blue,green \"npm:dev -w apps/server\" \"npm:dev -w apps/web\"",
    "dev:server": "npm run dev -w apps/server",
    "dev:web": "npm run dev -w apps/web",
    "typecheck": "npm run typecheck -w packages/shared && npm run typecheck -w apps/server && npm run typecheck -w apps/web",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  }
}
